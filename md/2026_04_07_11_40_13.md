# 업데이트 요약 (2026년 04월 07일)

현재까지 작업하여 변경된 주요 사항은 다음과 같습니다.

## 주요 변경 사항

1. **파일 구조 및 라우팅 개편**
   - 기존의 `index-teacher.html` 파일을 삭제하고, `index.html`을 통합 로그인 페이지 역할로 변경했습니다.
   - 학생과 교사 모두 첫 진입 시 통합 로그인 화면(`index.html`)을 거치도록 권한 분리 및 라우팅을 개선했습니다.

2. **React 앱 로직 분리**
   - `js/app_v13.js` 에서 LoginScreen 관련 코드를 분리하여 `index.html` 내에 직접 구현(LoginApp)하도록 수정했습니다.
   - 이를 통해 초기 로드 속도를 향상시키고 목적에 맞는 페이지 접속(예: student.html, teacher.html)으로 안내하도록 만들었습니다.

3. **커리큘럼 데이터 최적화 (`js/curriculum_v13.js`)**
   - 기존 커리큘럼 데이터에서 불필요한 문장들을 다수 제거하고, 핵심 문법 10개 단원에 대해 각각 4문장씩 총 40개의 최적화된 문장만 남겼습니다 (버전 13.1.1-final-pro).
   - 모든 문장의 ID에 `REF-` 접두사를 추가하여 고정 시스템 문장임을 명확하게 식별하도록 구조를 개선했습니다.
   - 각 문장에 대해 완벽한 S/V/O/C/M 구문 분석과 AI 프롬프트 역할을 지정했습니다.

4. **문서 업데이트 (README.md, history.md)**
   - `README.md`에 새로운 버전에 대응하는 변경된 HTML 파일 구조(student, teacher, login 등) 설명을 추가했습니다.
   - `docs/history.md`에 v13.1.1 변경 사항(데이터 최적화 완료, 고정 문장 REF 아이디 적용 등)을 기록했습니다.

이제 이 사항들을 GitHub `main` 브랜치에 안전하게 업로드(Push) 하도록 하겠습니다.
